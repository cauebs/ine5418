# INE 5418 - Trabalho 1

## Message queue:
To run the message queue: 

```
cargo run --bin mq
```

## Producer:
To run the producer:

```
cargo run --bin producer
```

## Consumer:
To run the consumer:

```
cargo run --bin consumer
```
